# .NET build output
bin/
obj/
*.user
*.suo
.vs/
project.lock.json
artifacts/

# IDE
.idea/
*.swp
*.swo
.vscode/

# macOS
.DS_Store

# Runtime state — NEVER commit these
# Anchored to repo root so src/KualiOnBase.Api/Data/ (source folder) isn't excluded
# on case-insensitive filesystems (Windows/macOS).
/data/
*.db
*.db-journal
*.db-shm
*.db-wal
logs/
backup/

# Secrets / local config overrides
appsettings.*.local.json
appsettings.Production.json
appsettings.Local.json
*.pfx
*.pem
*.key
.env
.env.*

# Claude local settings (contains per-user permission allowlist)
.claude/settings.local.json

# Test output
TestResults/
coverage/
*.trx

# AI assistant artifacts
context-snapshot.json

# ---------------------------------------------------------------------------
# THIS REPOSITORY IS PUBLIC. Nothing below may be committed — it names internal
# infrastructure (server hostnames, internal IPs, UNC shares, service accounts,
# app pool names) which is exactly the reconnaissance an attacker would want.
# Deployment docs that must be shared internally belong on the team wiki, not here.
# ---------------------------------------------------------------------------

# Publish output / deployment payloads (also contain a generated web.config)
publish/
kuali2ob-iis-publish/
*.zip

# Internal team wiki — not ours to publish
EApps-Wiki/

# Site-specific operational docs: real hostnames, IPs, shares, accounts.
# Operational scripts live outside the repo entirely (~/Desktop/kuali2ob-tools).
DEPLOY-IIS.md
